# ============================================================================
# fw3 (iptables) 叠加配置 —— 有意留空
#
# fw3 是 lede 的基线状态，不需要任何覆盖项：
#
#   - firewall / iptables / ip6tables / ipset / iptables-mod-* / kmod-ipt-*
#     全部来自 include/target.mk 的 DEFAULT_PACKAGES.router，默认即开
#   - luci-app-turboacc 同样在 DEFAULT_PACKAGES.router 里
#   - dnsmasq_full_ipset 在 dnsmasq/Makefile 里 default y
#
# 而 <target>.config 是 scripts/diffconfig.sh 的产物，只记录相对于原版
# make defconfig 的偏差——原版就是 fw3，所以偏差为零。
#
# 保留此文件是为了与 fw4.config 对称，说明「空」是结论而非遗漏。
# 若将来 fw3 侧需要固定某个选项（例如锁定某个 iptables 模块版本），
# 写在这里即可，无需改动 scripts/lib.sh。
#
# 对照：fw4 的差异清单见 fw4.config，验证记录见 docs/fw3-to-fw4-review.md
# ============================================================================
